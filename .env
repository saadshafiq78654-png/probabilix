ENVIRONMENT=DEV
DEBUG=1
CACHE=false
HMR=0# Random JWT token
JWT_TOKEN=bad3e09e3bb5ce4b569d24b6256fb5b4
DB_DRIVER=mysql
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
DB_NAME=aikeedo
DB_CHARSET='utf8mb4'
DB_UNIX_SOCKET=

# The filesystem path to the SQLite database
# Will used with following drivers: sqlite
SQLITE_PATH=

# COMPOSER_HOME
COMPOSER_HOME="${DOCUMENT_ROOT}/../var"

# PUBLIC DIRECTORY
# Possible values: public_html, public.
# PUBLIC_DIR=public_html

# THEME ASSETS SERVER
# THEME_ASSETS_SERVER=http://localhost:5174

# COOKIE DOMAIN
# When enabled, sets cookies for the specified domain.
# Must be used responsibly as it might break the authentication flow.
#
# Security considerations:
# - Setting a domain makes cookies available to all subdomains
# - Can break authentication if not configured correctly
# - Use with caution in production environments
#COOKIE_DOMAIN=